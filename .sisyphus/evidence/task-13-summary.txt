Task T13 evidence — Admin /plans monitoring endpoints

Implemented:
- GET /admin/plans returns the loaded Plans.list() array.
- GET /admin/plans/cost-summary?month=YYYY-MM aggregates completed request_logs by CLIProxy account and joins current account_subscriptions bindings to plan pricing metadata.
- GET /admin/plans/account/:account returns binding metadata plus the latest 50 request log usage rows.
- Structured admin access logs are emitted with events admin.plans.list, admin.plans.cost_summary, and admin.plans.account_view.

Unbound account policy:
- Included. Completed monthly request_logs with cliproxy_account but no account_subscriptions binding are returned with subscription_code=null, monthly_price_usd=0, and computed_overage_usd=total_cost_usd.

Verification:
- bun test: 73 pass, 17 skip, 0 fail, 337 expect() calls, 90 tests across 13 files.
- lsp_diagnostics src/: 0 errors (23 pre-existing hints).
- lsp_diagnostics tests/e2e/plans.test.ts: 0 diagnostics.
- bun run build: exit 0; bundled src/index.ts and src/cli.ts and copied migrations/plans data.
- Manual curl: temporary in-memory server on 127.0.0.1:13114 returned HTTP 200 for GET /admin/plans.

Deviations:
- None from the T13 endpoint scope. Quota, pass-through, supervisor, and cost modules were not modified.
