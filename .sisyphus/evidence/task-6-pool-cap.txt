# Task 6: Pool Cap Evidence

## Change: MAX_POOL_SIZE = 10,000 in src/cliproxy/correlator.ts

Applied after lookback filtering. Uses slice(-MAX_POOL_SIZE) to keep most recent entries.

## Test: pool capped to 10,000 when details exceed limit
- 20,000 details (10k "sliced-model" + 10k "kept-model")
- slice(-10000) removes all "sliced-model" entries
- Log matching "sliced-model" NOT correlated
- Log matching "kept-model" correlated
- PASS

## Typecheck: bun run typecheck — PASS (no errors)
