Task 10: QuotasPage redesign — pace insight verification

Build: PASS (bun run typecheck exit 0, bun run build exit 0)

Implementation verified:
- AccountQuotaCard computes pace insight per quota window
- Formula: hoursToLimit = ((100 - used) / used) * elapsedHours
- elapsedHours derived from resets_at and estimated window duration
- Window duration estimates: 5h=5h, week*=168h, other h/d suffixes parsed
- Pace only shown when used_pct > 0, reset time known, and not expired
- "At current pace, limit in ~Xh" shown when limit expected before reset
- "At current pace, within quota" shown when limit expected after reset or >999h
- "Expired" badge shown when resets_at < now (styled as critical status-badge)
- Color thresholds applied to progress bar and percentage text:
  - green (<50%), yellow (50-75%), orange (75-90%), red (>90%)
